#main program

def main():
    lang = 'python'
    #lang[0] = 'P'--generates a TypeError-we can't change a string
    print(lang[0])
    #concept of the length / size of the string
    print(len(lang))

main()